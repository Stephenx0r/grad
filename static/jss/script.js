// static/js/script.js

function generateQRCode() {
    // This function could be used to make an AJAX call to the server to generate a new QR code
    // and update the UI accordingly without needing to reload the page.
    alert("QR Code generation initiated!");
}